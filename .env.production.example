# Production Environment Configuration
# Copy this file to .env.production and update with your production values

# API Configuration
# IMPORTANT: This must be set at BUILD time for Docker deployments
# The value gets embedded into the client-side JavaScript bundle
NEXT_PUBLIC_API_URL="https://api.yourproductiondomain.com"

# Docker Build Example:
# docker build --build-arg NEXT_PUBLIC_API_URL="https://api.yourproductiondomain.com" -t next-ai .
#
# Or use docker-compose with .env file:
# Create a .env file in the project root with:
# NEXT_PUBLIC_API_URL=https://api.yourproductiondomain.com
# Then run: docker-compose up --build
