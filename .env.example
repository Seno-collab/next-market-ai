# === API Configuration ===

# Public URL — embedded into client bundle at BUILD time.
# Browser uses this to call the backend REST API directly.
# Development: http://localhost:8080
# Production:  https://api.yourdomain.com
NEXT_PUBLIC_API_URL="http://localhost:8080"

# WebSocket base URL (optional — derived from NEXT_PUBLIC_API_URL if omitted).
# Set explicitly if WS runs on a different host/port than the REST API.
# Development: ws://localhost:8080
# Production:  omit (same-origin via Nginx handles it)
# NEXT_PUBLIC_WS_BASE_URL="ws://localhost:8080"

# Internal URL — used by Next.js server-side (SSR) at RUNTIME.
# Points to backend nginx via shared docker network.
# Development: http://localhost:8080
# Production:  http://nginx:80
API_URL="http://localhost:8080"
